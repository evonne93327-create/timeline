# 開發筆記（交接用）

給接手的人／下一個對話看的。README 講「這個 app 是什麼」，這份講
**「改它的時候要知道什麼」**——主要是踩過坑之後才立下的規則，
以及還沒做完的事。

姊妹專案：[`evonne93327-create/world_2`](https://github.com/evonne93327-create/world_2)（世界觀架構工作台：文檔 + 關係白板）。
下面「硬規則」與「工作方式」兩節是從那邊搬過來的，兩邊是同一份，
改了記得兩邊一起改。這個專案的配色、彈窗行為、service worker 策略
也都沿用那邊——不是偷懶，是那些規則在那邊是出過事才立的。

---

## 現況

| | |
|---|---|
| 線上位置 | https://evonne93327-create.github.io/timeline/ |
| main | `b813561` |
| service worker | **v4** |
| 分支流程 | 沒有，直接在 main 上開發 |

### 這個 app 沒有雲端

資料只在使用者這台裝置的 `localStorage` 裡。**沒有帳號、沒有伺服器、
沒有同步。** 所以「匯出備份」不是加分功能，是唯一的救命繩——任何會
動到既有資料的功能（匯入、批次刪除）都要先讓使用者拿到一份備份。

現在的匯入就是這樣做的：整包取代之前自動下載一份目前的資料。

---

## 這個專案特有的設計決定

### 排序不吃「時間」欄位

年表的時間多半是「舊曆340年」「第三紀元 12 年」這種自由文字，沒辦法
直接比大小。所以每個事件另外有一個 `sort` 數字決定先後。

- 新事件預設接在最後一筆 `+ SORT_STEP`（10）。**留間隔是為了讓
  「插在這兩件事中間」可以直接填一個中間值，不必把後面全部重編號。**
- 「從時間猜」把文字裡第一串數字抓出來當**建議值**，刻意不自動套用：
  「第三紀元 12 年」抓到的是 3 不是 12，猜錯的時候使用者要看得到、改得動。
- 排序值留空要接在最後面。注意 **`Number("")` 是 `0` 不是 `NaN`**，
  直接丟給 `isFinite()` 會判定成「有填，值是 0」，那筆就跑到最前面去了。
  第一版就是錯在這裡，測試抓到的。

### 版面是工作台那一套，不是自己長出來的

```
桌機（>768px）                       手機（≤768px 或橫放 max-height:500px）
┌──┬───────┬────────────┐      ┌──────────────────┐
│直│ 時間軸  │ 頂欄        │      │ ☰ 名牌       ＋  │ ← 同一條頂欄
│欄│ 側欄    ├────────────┤      ├──────────────────┤
│62│ 280px  │ 內容（會捲） │      │ 內容（會捲）      │
│  │        │            │      ├──────────────────┤
│⚙️│ ＋新增  │            │      │ 📜 🌍         ⚙️ │ ← 直欄折成底部橫列
└──┴───────┴────────────┘      └──────────────────┘
                                  側欄 → 從左邊滑出的抽屜 + 遮罩
```

**同一份 DOM，兩種版面只差 CSS。** 不是做兩套再各自顯示一半——所以不會有
「在手機上改了名字、轉桌機時側欄還寫著舊名字」那種事，`renderAll()` 只要
更新一組元素。

分界 **768px**（外加 `max-height: 500px` 給橫放的手機），跟工作台同一個值。
不是這個專案原本的 900px：版面整個換成那邊的，分界也要一起換。

幾個看起來可以刪、其實不能刪的：

| 寫法 | 為什麼必要 |
|---|---|
| `body` 的 `height:100dvh` 擺在 `100vh` 後面 | 手機網址列縮放時 `vh` 不會跟著變，底部橫列會被蓋掉一半 |
| `body { overflow: hidden }` | 會捲的是 `.sidebar-list` 與 `.content-area`。整頁一起捲的話，捲到一半連「現在在哪條時間軸」都看不到 |
| `.main-layout { min-width: 0 }` | flex 項目預設是 `min-width:auto`，縮不回去；長標題會把主區推寬，橫向捲軸就冒出來了 |
| 抽屜是 `top:0; bottom:0`（整個螢幕高） | 只拉到底部橫列上緣的話，抽屜下面會露出半截橫列，看起來像少切了一塊。橫列比抽屜寬（85vw 上限 320px），右邊那顆 ⚙️ 本來就還露在外面，按得到 |
| `.tl-node` 基底那圈 `1px solid transparent` | 選中時會多一條 1px 的邊，沒先佔位整列會往下跳一格 |
| 手機版 `.rail-foot-btn` 把 `margin-top:auto` 換成 `margin-left:auto` | 直向是推到最下面，折成橫列要推到最右邊 |

**「新增事件」只有頂欄右邊那一顆**，手機桌機同一顆（手機上只把「新增事件」
四個字收起來，留 ＋）。右下角那顆浮動按鈕拿掉了——工作台也是把「新增」
放在頂欄，而且底部橫列一佔位，浮動按鈕就沒地方站了。所以空狀態的說明
兩邊講同一句話，不必再依版面分岔。

視窗跨過分界時要 `closeSidebarDrawer()`：抽屜開著把視窗拉寬的話，
`.drawer-open` 會留在那裡，桌機的 CSS 不理它，畫面看起來正常，
但遮罩還在，點哪裡都沒反應。用 `matchMedia` 的 `change` 不要用 `resize`
（拖動視窗會被呼叫上百次）。

### 時間軸的編輯視窗有兩個來源

`openTimelineEditModal(id, fromList)` 的第二個參數決定關掉時要不要回
「時間軸清單」。舊版面只有清單一個入口，無條件回清單沒錯；現在側欄的 ✎、
側欄的 ＋、頂欄的名牌都會開這個視窗，無條件回清單就變成
**「按了取消反而多跳出一個視窗」**。

### 兩個 app 是同一個網域下的兄弟資料夾

```
https://evonne93327-create.github.io/timeline/    ← 這裡
https://evonne93327-create.github.io/world_2/     ← 姊妹 app
                                    ↑ 網域相同
```

GitHub Pages 是「一個 repo 一個資料夾」，所以這兩個 app **對瀏覽器來說
早就是同一個網站**了（同源看的是網域，不看後面的路徑）。兩件事跟著這個事實走：

- **`localStorage` 是共用的同一個抽屜。** 沒有互相蓋掉純粹是因為 key 名字
  剛好不一樣（這邊 `timeline_*`／`wb` 那邊是 `novel_*`、`world_*`、`wb_theme`）。
  **以後加新的 key 一定要帶 `timeline_` 前綴**，不然會直接踩到那邊的資料。
- **互跳只要相對路徑。** 側邊欄底部與設定總表各有一個入口，寫的是
  `../world_2/`。不要改成完整網址——本機開發跳不過去，repo 改名也會爛掉。
  第 16 組測試守這條。

> 那邊還沒有跳回來的按鈕，要補的話是同一份東西反過來（`../timeline/`），
> 外加 `a.settings-row { text-decoration: none; color: var(--text-primary); }`。

合併成一個 repo 的事討論過，**目前不做**。代價是：網址變了書籤全失效、
裝起來的 PWA 要重裝、舊的 service worker 還會留在使用者瀏覽器裡，而且
world_2 的 Google Drive OAuth redirect 跟 Supabase 的允許網址是綁死網址的，
路徑一改就要去後台改設定，沒改就登不進去。

### 彈窗的樣式是跟 world_2 對齊的，不是各做各的

整組彈窗——遮罩、卡片、標題列、按鈕、表單欄位、設定列、主題三選一——
都照姊妹專案抄過來，同一類型的彈窗長得一模一樣：

| | 值 |
|---|---|
| 遮罩 | `rgba(42,36,32,0.45)` + `blur(4px)`。純黑配這組暖調米色會發灰 |
| 卡片 | `width` 固定 + `max-width:100%`、內距 `22`、圓角 `lg`、`max-height:85vh` |
| 每個彈窗的寬 | 表單 460（事件、存檔失敗）／總表 440／單選 400（外觀）／通知 420 |
| 標題 | `16px` 700 襯線（不是 18） |
| 按鈕 | 底色與邊框都透明，由 `-primary`／`-secondary`／`-danger` 決定長相；`12px` 600、內距 `7/14` |
| 欄位標籤 | `12px` 次要色、**不加粗** |
| 設定列 | 卡片底色 + 大圓角（`lg`），hover 才變 sunken |

刻意沒跟過去的只有兩個地方，都寫在 CSS 的註解裡：

- **輸入框還是 16px**（那邊是 14px）。16px 以下 iOS Safari 一聚焦就把整頁放大，
  這個 app 主要在手機上用，不能為了對齊字級把那個毛病收回來。
- **電腦版標題列的「新增事件」不跟著縮到 12px**。它是頁面主要的行動點，
  不是彈窗裡的按鈕——工作台那邊的工具列按鈕也是自己一套尺寸。

第 15 組測試守的是前面那張表。要改樣式沒問題，但兩邊要一起改，
不然「同一個系列」這件事就只剩嘴上說說。

> world_2 只是拿來對照的，**不要動它**。

---

## 硬規則

這幾條都是在姊妹專案出過事才立的，不要為了「比較簡潔」退回去。

### 1. localStorage 一律走 `safeStorage*`（`js/state.js`）

不是只有「空間滿了」一種壞法。瀏覽器設定關掉網站資料、企業政策、某些嚴格的
隱私模式下，**光是讀 `window.localStorage` 這個屬性本身就會丟 `SecurityError`**。
沒包起來的話，一丟例外整個檔案就在那行中斷，後面的宣告全部沒執行到；
函式因為提升看起來還在，一呼叫就撞上 TDZ。

結果是 app 看起來完全正常，但每次存檔都在背景丟例外、什麼都沒存進去，
而且不會告訴使用者。

### 2. Service worker：網路優先、不碰跨網域、改了就加 VERSION

- **網路優先、離線才回退快取。** 快取優先會把使用者鎖在舊版程式碼裡，
  而且很難自己救回來——強制重新整理也未必有用，因為回應是 service worker
  給的，根本沒碰到網路。
- **絕不快取跨網域請求。**
- **動到 `index.html` 引用的任何檔案，就把 `sw.js` 的 `VERSION` 加一號。**
  `tests/browser.test.js` 的第 9 組會檢查 SHELL 有沒有跟實際檔案脫節，
  但版號要自己記得改。

### 3. 觸控裝置不自動聚焦輸入框

會把軟體鍵盤叫出來，鍵盤又把彈窗擠掉半個畫面。判斷用
`isTouchPrimary()`（`(hover: none) and (pointer: coarse)`），
**不要用寬度**——iPad 橫放超過 768px 但一樣沒有實體鍵盤。

### 4. 彈窗卡片不要畫預設焦點框

`.modal-card` 的 `tabindex="-1"` 只是給程式聚焦用的錨點，使用者按 Tab
到不了它。不關掉 `outline` 的話，瀏覽器會在整張卡片外圍畫一圈
（深色主題下 Chromium 畫出來是金黃色），看起來像那個彈窗的外框壞了。

### 5. 使用者輸入一律 `createElement` + `textContent`

不要拼 `innerHTML`。標題與內文可能是匯入來的，拼進 HTML 就等於把
`"` `<` 這些字元交給瀏覽器解讀。用 `textContent` 從源頭就沒有這個問題，
不必每個欄位都記得 escape（漏掉一個就是一個洞）。

### 6. 匯入的資料一律重組，不要直接接上

`validateImported()` 是白名單：只留認得的欄位、型別不對就換成預設值。
直接把原物件接上去的話，多帶的欄位會一路跟著存進 localStorage。
另外要丟掉掛在不存在時間軸上的事件——那種永遠看不到，留著只是垃圾。

---

## 工作方式

- **每個改動都用 Playwright 實測**，不要只看程式碼覺得對。
- **故意把修好的地方改壞，確認對應的測試真的變紅。** 好幾次測試是空的
  （斷言寫錯、選擇器選不到、或被更前面的條件先擋掉了），不驗證等於沒測。
  這個專案裡「N 鍵在輸入框中不攔截」那條第一版就測不到，因為被
  「有彈窗開著就不攔」先擋掉了。
- **註解寫「為什麼」不寫「做什麼」**，尤其是踩過的坑與刻意的取捨。
- **量，不要猜。** 有好幾次「截圖看起來對齊」的版面實測差 185px。

---

## 測試

```bash
python3 -m http.server 8800 &
node tests/browser.test.js      # 125 項
```

Playwright 在這個環境的路徑：

```js
const { chromium } = require('/opt/node22/lib/node_modules/playwright');
const b = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium' });
```

| 組 | 測什麼 |
|---|---|
| 1 | 事件的新增、排序、編輯、刪除、重新載入後還在 |
| 2 | 排序值：留空、亂填、從時間猜 |
| 3 | 多條時間軸彼此獨立 |
| 4 | 匯入的驗證（孤兒事件、奇怪的 id、不認得的分類色、多帶的欄位） |
| 5 | 使用者打的字不會被當成 HTML |
| 6 | 主題切換（分類色是 JS 寫進 style 的，切主題一定要重畫） |
| 7 | 手機的返回鍵 |
| 8 | 觸控裝置打開彈窗不要跳鍵盤 |
| 9 | sw.js 的 SHELL 跟實際檔案一致 |
| 10 | 768px 兩側：側欄常駐／變抽屜、直欄轉向、按鈕文字收起來 |
| 11 | 側欄切換（含「按 ✎ 不該順便切換」與三種入口的「取消」行為） |
| 12 | N 鍵的四種情況 |
| 13 | 內容欄在主區裡置中、寬度有上限 |
| 14 | 空狀態指向真的存在的那顆按鈕 |
| 15 | 彈窗樣式沒有跟 world_2 走鐘（寬度、遮罩、內距、殘留的舊類別名） |
| 16 | 切換到 world_2 的入口（相對路徑、沒有連結底線、直欄上的大小） |
| 17 | 手機抽屜（滑出、遮罩、整個螢幕高、選完收起來、返回鍵） |

**WebKit 裝不起來**（proxy 擋掉 `cdn.playwright.dev`），所以 Safari／iOS
專屬的問題只能靠使用者實機回報。

---

## 還沒做的

使用者提過、或我提議過但還沒決定的：

| 項目 | 備註 |
|---|---|
| 事件搜尋 | |
| 依分類篩選 | 七種分類已經有了，只差篩選的 UI |
| 拖曳排序 | 現在只能改排序數字。手機上拖曳很難做好，要想清楚 |
| 紀元／分段標題 | 「第一紀元」「舊曆」這種分段，會影響排序的資料結構 |
| 跟 world_2 互通 | **資料目前刻意不互通**，使用者選的是「完全獨立」方案（互跳的連結有了，那只是換頁）。之後要做的話，得先在 world_2 那邊給文檔加「事件時間」欄位 |
| world_2 那邊補跳回來的入口 | 這邊已經有去程，回程要在那個 repo 改 |

## 待使用者實機驗證

- **新增事件 → 關掉 app → 重開**，確認資料真的留著。
  這個 app 沒有雲端，存檔壞掉就什麼都沒了，這是最重要的一項。
- 手機上按返回鍵關彈窗（應該收掉彈窗而不是離開 app）。
- 設定 → 匯出備份，確認檔案真的下載得下來。
- **兩個 app 並排開著，看版面與彈窗是不是真的長一樣。**
  這輪是照 world_2 改的，桌機與手機都用 Chromium 量過了，
  但 Safari／iOS 上的 `backdrop-filter` 與 `100dvh` 只能靠實機看。
- **手機上開抽屜 → 按返回鍵**（應該收抽屜，不是離開 app）。
- **手機轉橫向**（`max-height:500px` 也算手機版，底部橫列要還在）。
- **裝成 PWA 之後看底部橫列有沒有被 home bar 蓋到**（`safe-area-inset-bottom`）。
